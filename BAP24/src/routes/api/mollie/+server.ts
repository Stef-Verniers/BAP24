import { prisma } from "$lib/server/prisma";
import { json } from "@sveltejs/kit";

// Mollie integratie... 
export async function POST({ request }) {
    try {

    } catch (error) {
      console.error(error);
      };
}